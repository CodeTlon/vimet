# OPEN_QUESTIONS — VIMET (web)

Preguntas que no se pudieron cerrar desde este entorno — quién puede confirmarlas, y qué hacer con la respuesta. Varias se cerraron el 2026-09-22 con acceso directo a la API de Coolify (token provisto por el usuario) y lectura directa de la DB de producción real.

1. **¿Coolify auto-deploya en push a `main`, o requiere un trigger manual?** Todavía sin confirmar — se confirmó identidad del proyecto/app en Coolify (ver #3, ya cerrada) pero no el mecanismo de trigger de deploy en sí. **Quién confirma**: quien tenga acceso al dashboard de Coolify (`http://51.91.253.54:8000/`, app `vimet`, uuid `ukzph5jqj5fxsl96xzyffeh7`).
2. ~~¿El host de Coolify corre en Windows o Linux?~~ Ya no es bloqueante de seguridad — el RCE de `KNOWN_ISSUES.md` #5 se parchó independientemente del OS del host.
3. ~~¿Coolify usa el mismo proyecto de Supabase que dev, o uno separado?~~ **Cerrada con evidencia dura, 2026-09-22.** Consultada la API de Coolify (`GET /api/v1/applications/{uuid}/envs` sobre la app `vimet`): `NEXT_PUBLIC_SUPABASE_URL=https://ayjzcxvtylvsjacgjxgh.supabase.co` — separado de `vimet-dev` (`qwzlhbecpgysgophpbyf`). Confirma también `KNOWN_ISSUES.md` #1 (el gap de migraciones): ese proyecto de prod le falta un bloque de 6 migraciones que sí están en dev.
4. ~~¿`RESEND_FROM_EMAIL` en Coolify ya usa el dominio verificado?~~ **Cerrada, 2026-09-22.** Confirmado vía la misma consulta a Coolify: `RESEND_FROM_EMAIL=no-reply@vimetsalud.com.ar` — ya está bien en prod.
5. ~~¿Las migraciones `0038`/`0039` y su backfill corrieron contra el Supabase real?~~ **Cerrada, 2026-09-22, por lectura directa.** Ver `KNOWN_ISSUES.md` #4: en prod no hay ningún dato clínico cargado todavía (0 filas), así que la pregunta de si el backfill corrió es hoy irrelevante — no hay nada que backfillear. Sigue siendo relevante para el futuro, una vez que haya uso real.
6. **¿Se decidió a propósito no cifrar `plan_secciones`, o es un olvido del commit `6af5db6`?** Sigue abierta — ver `KNOWN_ISSUES.md` #2. **Quién confirma**: el usuario/cliente (Avril/Gero), aunque ahora hay margen porque todavía no hay dato real en esa tabla en prod.
7. **¿El proyecto Vercel debería decomisionarse, o se usa a propósito como preview?** Sigue abierta — decisión de producto/infra del usuario.
8. ~~¿Qué es el proyecto Supabase `vimet` (`ayjzcxvtylvsjacgjxgh`)?~~ **Cerrada, 2026-09-22** — es el Supabase real de producción, confirmado vía Coolify (ver #3).
9. **¿Se aplican las 6 migraciones faltantes (`KNOWN_ISSUES.md` #1) contra producción?** El análisis de riesgo ya está hecho (las 6 son seguras estructuralmente; `0033` necesita el backfill corregido con los emails reales) — falta la autorización explícita del usuario para escribir contra prod. **Ninguna se aplicó todavía.**
10. **`vimet-dev` (`qwzlhbecpgysgophpbyf`) quedó reactivado** (estaba pausado) tras la verificación de `activo` de esta sesión — no se volvió a pausar. El usuario puede pausarlo manualmente si prefiere no dejarlo corriendo; Supabase free-tier lo vuelve a pausar solo tras inactividad prolongada.
