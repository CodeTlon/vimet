# OPEN_QUESTIONS — VIMET (web)

Preguntas que no se pudieron cerrar desde este entorno (sin acceso a Coolify, sin acceso de escritura a Supabase de prod) — quién puede confirmarlas, y qué hacer con la respuesta.

1. **¿Coolify auto-deploya en push a `main`, o requiere un trigger manual?** No hay webhook clásico ni GitHub App de Coolify visible desde `gh api repos/CodeTlon/vimet/hooks` / `orgs/CodeTlon/installations` — el dominio real responde (verificado), pero el mecanismo de trigger no. **Quién confirma**: quien tenga acceso al dashboard de Coolify. Si es manual, vale la pena documentarlo explícitamente para que nadie asuma que un `git push` alcanza.
2. ~~¿El host de Coolify corre en Windows o Linux?~~ Ya no es bloqueante de seguridad — el RCE #1 de `KNOWN_ISSUES.md` se parchó (bump de Next.js) independientemente del OS del host. Sigue siendo un dato de infra útil de tener, sin urgencia.
3. **¿Coolify usa el mismo proyecto de Supabase que dev, o uno separado de producción?** El propio `.claude/CLAUDE.md` ya lo marca como sin confirmar. Afecta si hay que replicar la config de Resend/SMTP/templates/env vars en un segundo lugar. **Quién confirma**: acceso a Coolify + Supabase dashboard.
4. **¿`RESEND_FROM_EMAIL` en el entorno real de Coolify ya usa el dominio verificado, o sigue en `onboarding@resend.dev`?** El fix se aplicó y confirmó en `.env.local` (dev) el 09/09, pero no se replicó en el entorno de Coolify en esa sesión. **Quién confirma**: acceso a las env vars de Coolify.
5. **¿Las migraciones `0038`/`0039` (cifrado clínico + audit log) y su script de backfill corrieron contra el Supabase real (dev y/o prod)?** Ver `KNOWN_ISSUES.md` #5 — sin esto, no se puede afirmar si los datos clínicos históricos siguen en texto plano. **Quién confirma**: correr `supabase/scripts/verificar_migraciones_aplicadas.sql` (de solo lectura) contra cada entorno — requiere autorización explícita para leer (no escribir) esos entornos.
6. **¿Se decidió a propósito no cifrar `plan_secciones`, o es un olvido del commit `6af5db6`?** Ver `KNOWN_ISSUES.md` #3. **Quién confirma**: el usuario/cliente (Avril/Gero) según qué tan sensible consideren el contenido de un plan de comidas.
7. **¿El proyecto Vercel debería decomisionarse, o se usa a propósito como ambiente de preview?** Hoy existe sin servir el dominio real y sin estar claro si alguien lo revisa. **Quién confirma**: el usuario, es una decisión de producto/infra, no algo que el código resuelva.
8. **¿Qué es el proyecto Supabase `vimet` (`ayjzcxvtylvsjacgjxgh`, `ACTIVE_HEALTHY`, creado 2026-08-05)?** Destapado el 2026-09-22 al reactivar `vimet-dev` para verificar el fix de `activo` — ningún `.env*` local lo referencia. Candidatos: (a) es el Supabase real que usa Coolify en producción (respondería la pregunta #3 de este archivo), (b) es un dev/staging nuevo que reemplazó a `vimet-dev` sin actualizar `.env.local`, (c) otra cosa. No se tocó ni se leyó su contenido en esta sesión — se necesita confirmación antes de siquiera inspeccionarlo, por la chance real de que sea producción. **Quién confirma**: el usuario, revisando el dashboard de Supabase (organización `fcwltzkaggjvjqfytncx`) o el panel de Coolify (qué `NEXT_PUBLIC_SUPABASE_URL` tiene seteada).
9. **`vimet-dev` (`qwzlhbecpgysgophpbyf`) quedó reactivado tras la verificación de esta sesión** (estaba `INACTIVE`/pausado). No se volvió a pausar — Supabase free-tier lo pausa solo de nuevo tras inactividad prolongada, así que no es urgente, pero el usuario puede pausarlo manualmente desde el dashboard si prefiere no dejarlo corriendo.
